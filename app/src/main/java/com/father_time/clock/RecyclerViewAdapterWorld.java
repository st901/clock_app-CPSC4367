package com.father_time.clock;

/*public class RecyclerViewAdapterWorld extends RecyclerView.Adapter<RecyclerViewHolderWorld> {
    private Random random;

    public RecyclerViewAdapterWorld(int seed) {
        this.random = new Random(seed);
    }

    @Override
    public int getItemViewType(final int position) {
        return R.layout.list_world;
    }

    @NonNull
    @Override
    public RecyclerViewHolderWorld onCreateViewHolder(@NonNull ViewGroup parent, int viewType) {

        View view = LayoutInflater.from(parent.getContext()).inflate(viewType, parent, false);
        return new RecyclerViewHolderWorld(view);
    }

    @Override
    public void onBindViewHolder(@NonNull RecyclerViewHolderWorld holder, int position) {
        holder.getView().setText(String.valueOf(random.nextInt()));
    }

    @Override
    public int getItemCount() {
        return 100;
    }
}
*/